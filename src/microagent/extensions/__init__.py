"""Convenience extensions shipped alongside the core.

These are thin, zero-dependency helpers — preset handoff filters, a recommended prompt
prefix for multi-agent setups, and an optional graph visualization helper (requires
`graphviz`). Addons with heavyweight deps (sandbox, MCP, tracing exporters) live in the
`microagent-*` addon ecosystem instead.
"""

__all__: list[str] = []